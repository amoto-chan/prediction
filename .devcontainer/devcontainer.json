{
    "forwardPorts": [8000],
    "postStartCommand": "php artisan serve --host=0.0.0.0 --port=8000 > storage/logs/dev-server.log 2>&1 &",
    "portsAttributes": {
        "8000": {
            "label": "Laravel",
            "onAutoForward": "openBrowser"
        }
    }
}